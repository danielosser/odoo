/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            replyingToMessageComposer
        [Element/model]
            DiscussComponent
`;
