/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            body
        [Element/model]
            FollowerSubtypeListComponent
        [web.Element/tag]
            main
        [web.Element/class]
            modal-body
`;
