/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            inlineText
        [Element/model]
            NotificationListItemComponent
        [web.Element/class]
            text-truncate
`;
