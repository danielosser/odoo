/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            participantCard
        [Element/model]
            RtcCallViewerComponent
`;
