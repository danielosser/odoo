/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            actionButton
        [Element/model]
            ComposerViewComponent
`;
