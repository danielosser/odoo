/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Test}
        [Test/name]
            create (chat)
        [Test/model]
            Thread
        [Test/assertions]
            15
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/traits]
                        Env
            @testEnv
            .{Record/insert}
                [Record/traits]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
                    .{isFalsy}
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Thread/id]
                            200
                        [Thread/model]
                            mail.channel
                    .{isFalsy}

            :channel
                @testEnv
                .{Record/insert}
                    [Record/traits]
                        Thread
                    [Thread/channelType]
                        chat
                    [Thread/id]
                        200
                    [Thread/members]
                        @testEnv
                        .{Field/add}
                            {Record/insert}
                                [Record/traits]
                                    Partner
                                [Partner/email]
                                    demo@example.com
                                [Partner/id]
                                    5
                                [Partner/imStatus]
                                    online
                                [Partner/name]
                                    Demo
                    [Thread/model]
                        mail.channel
            {Test/assert}
                [0]
                    @record
                [1]
                    @channel
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Thread/id]
                            200
                        [Thread/model]
                            mail.channel
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
            {Test/assert}
                [0]
                    @record
                [1]
                    @channel
                    .{=}
                        @testEnv
                        .{Record/findById}
                            [Thread/id]
                                200
                            [Thread/model]
                                mail.channel
            {Test/assert}
                [0]
                    @record
                [1]
                    @channel
                    .{Thread/model}
                    .{=}
                        mail.channel
            {Test/assert}
                [0]
                    @record
                [1]
                    @channel
                    .{Thread/channelType}
                    .{=}
                        chat
            {Test/assert}
                [0]
                    @record
                [1]
                    @channel
                    .{Thread/id}
                    .{=}
                        200
            {Test/assert}
                [0]
                    @record
                [1]
                    @channel
                    .{Thread/correspondent}
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
                    .{=}
                        @channel
                        .{Thread/correspondent}
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
                    .{Partner/email}
                    .{=}
                        demo@example.com
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
                    .{Partner/id}
                    .{=}
                        5
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
                    .{Partner/imStatus}
                    .{=}
                        online
            {Test/assert}
                [0]
                    @record
                [1]
                    @testEnv
                    .{Record/findById}
                        [Partner/id]
                            5
                    .{Partner/name}
                    .{=}
                        Demo
`;
