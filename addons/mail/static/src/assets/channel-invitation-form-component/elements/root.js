/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            root
        [Element/model]
            ChannelInvitationFormComponent
        [web.Element/class]
            d-flex
            flex-column
`;
