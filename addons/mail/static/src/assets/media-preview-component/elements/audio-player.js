/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Element}
        [Element/name]
            disableVideoButton
        [Element/model]
            MediaPreviewComponent
        [web.Element/tag]
            audio
`;
